node_modules/
dist/
*.tsbuildinfo

# локальные настройки Claude Code (у каждого свои)
.claude/settings.local.json

# промежуточные файлы оцифровки чартов
tools/__pycache__/
page*.json
*.png

# ...но исходники MTT-пака FF START — версионируем: без них чарты не
# перегенерировать. Отладочные оверлеи (--debug) по-прежнему мимо.
!charts/**/*.png
charts/**/*.debug.png

# выгрузки истории рук GGPoker — данные, а не исходники
*.zip

.DS_Store
